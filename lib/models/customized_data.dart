import 'dart:ui';

class CustomizedData {
  String title;
  double textSize;
  bool colorChangeAnimation;
  Color colorText;
  CustomizedData({this.title, this.colorChangeAnimation, this.textSize, this.colorText});
}